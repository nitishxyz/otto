---
import BaseLayout from '../layouts/BaseLayout.astro';
import { OttoRouter as OttoRouterPage } from '../react-pages/OttoRouter';
---

<BaseLayout
	title="OttoRouter — otto"
	description="One account for every frontier model. Pay per request from a single balance, switch models anytime."
	ogTitle="OttoRouter"
	ogSection="OttoRouter"
	ogDescription="One account for every frontier model. Pay per request from a single balance — no per-provider API keys to juggle."
>
	<OttoRouterPage client:load />
</BaseLayout>
