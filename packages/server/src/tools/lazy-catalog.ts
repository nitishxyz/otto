export const SERVER_LAZY_TOOL_CATALOG = [
	{
		name: 'forge',
		description:
			'Explicitly create, manage, and run Otto extensions, MCP servers, and plugin commands; never use for ordinary project work.',
	},
] as const;
