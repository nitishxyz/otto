Display and maintain a concise task plan for non-trivial multi-step work. Skip it for simple requests.

Keep exactly one item `in_progress`. Mark each item `completed` immediately when finished, update the list when scope changes, and use short imperative descriptions. The UI displays the plan automatically, so do not repeat it in prose.
