Apply several exact replacements to one existing file atomically. Read first and copy each `oldString` from current content. A unique whole-line whitespace-normalized fallback is allowed, but non-whitespace differences are never guessed. Every edit must succeed or none are written; use `replaceAll` only when every match should change.
