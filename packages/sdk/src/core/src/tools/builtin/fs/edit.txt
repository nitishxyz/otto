Replace one exact text block in an existing file. Read first and copy `oldString` from current content. If it is not exact, a unique whole-line whitespace-normalized match may be used; non-whitespace differences are never guessed. Add context when the text repeats, or use `replaceAll` only when every match should change.
