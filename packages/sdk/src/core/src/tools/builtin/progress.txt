Show a short user-facing status at major phase changes. Keep messages specific, actionable, and under 80 characters; avoid updates for every tool call. Optional `pct` is 0–100. Choose the matching stage: `planning`, `discovering`, `generating`, `preparing`, `writing`, or `verifying`.
